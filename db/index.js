//this will have the queries
const connection = require('./connection.js');


//query for presented with a formatted table showing department names and department ids
const getRole = connection.query(``);

//query presented with the job title, role id, the department that role belongs to, and the salary for that role